# skills

该目录存放 `code-explorer` 插件的技能包。

当前技能：

- `code-explorer-workspace`：规范 `.code-explorer/` 工作区目录与 XML 状态文件写法
- `code-explorer-output-style`：约束教学型文档写法

skills 用于约束工作流的输出和状态流转，不代替 commands 与 agents。
