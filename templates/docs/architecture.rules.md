# Architecture Template Rules

该规则文件约束 `SYSTEM_ARCHITECTURE.md`。

## 强制要求

1. 必须包含目录。
2. 必须包含 Mermaid 组件图。
3. 必须包含 Mermaid 时序图。
4. 必须至少包含 2 个代码块，并解释这些代码为什么是架构关键节点。
5. 必须明确推荐读者优先阅读的文档或模块。
6. 文末必须返回总索引。

## 写作重点

- 重点回答“系统整体在做什么”和“各层如何协作”。
- 架构说明不能停留在目录级描述。
- 关键代码节点要解释设计意图，而不是只贴函数体。
