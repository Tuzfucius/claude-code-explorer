# XML Template Rules

该规则文件约束 `.code-explorer/` 中 XML 文件的结构一致性。

## 适用对象

- `PHASE_*_STATUS.xml`
- `WAVE_*_PLANS.xml`
- `INDEX_MAP.xml`

## 强制要求

1. 必须遵循模板字段顺序，避免阶段之间字段漂移。
2. 缺失信息时也保留节点，必要时填空字符串或空数组结构。
3. 阶段状态文件和计划文件命名必须与工作区规范完全一致。
4. 新增字段前，先更新对应模板和工作区技能说明。
